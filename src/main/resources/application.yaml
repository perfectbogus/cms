spring:
  datasource:
    url: jdbc:postgressql://localhost:5432/postgres
    username: postgres
    password: cms@spring
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true